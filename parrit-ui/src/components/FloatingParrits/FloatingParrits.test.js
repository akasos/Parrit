import React from 'react';
import { shallow } from 'enzyme';
import FloatingParrits from "./FloatingParrits";

describe('FloatingParrits', () => {
    describe('Adding Person', () => {
        it('should call `addPerson` func, when the a', ()=> {});
    });
});